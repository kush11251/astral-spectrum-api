# Astral Spectrum API

A fast and scalable API for managing astronomical spectra

## Prerequisites
* Install Go (version 1.19 or higher)
* Initialize Go module: `go mod init astral-spectrum-api`

## Build and Run
* Run `go build src/main.go` to compile the code
* Run `go run src/main.go` to start the API server